{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "uv-sync",
            "type": "shell",
            "command": "uv sync",
            "options": {
                "cwd": "${workspaceFolder}/orchestration/complaint_workflow"
            },
            "problemMatcher": []
        }
    ]
}
