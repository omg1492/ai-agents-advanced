[project]
name = "complaint-workflow"
version = "0.1.0"
description = "Temporal-based complaint handling workflow with LLM integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "temporalio>=1.8.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
    "openai>=1.59.0",  # Azure OpenAI structured outputs
]


