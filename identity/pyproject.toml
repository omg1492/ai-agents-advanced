[project]
name = "identity"
version = "0.1.0"
description = "Identity provisioning utilities (Keycloak)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27.0,<0.28.0"
  ,"python-dotenv>=1.0.1,<2.0.0"
]
