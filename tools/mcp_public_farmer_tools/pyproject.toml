[project]
name = "mcp-public-farmer-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"fastmcp>=2.0.0",
	"python-dotenv>=1.0.1",
	# OpenTelemetry core and exporters
	"opentelemetry-api>=1.20.0",
	"opentelemetry-sdk>=1.20.0",
	"opentelemetry-exporter-otlp-proto-grpc>=1.20.0",
	# OpenTelemetry auto-instrumentation for Starlette
	"opentelemetry-instrumentation-starlette>=0.41b0",
]

[tool.uv]
package = true

[tool.uv.sources]
# No custom sources required; using PyPI

#############################################
# Tests are deferred to a dedicated MCP test framework
# (kept out of this subproject to avoid low-level harnesses).

