apiVersion: v1
kind: Service
metadata:
  name: mcp-public-farmer-tools
  labels:
    app: mcp-public-farmer-tools
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: mcp-public-farmer-tools
