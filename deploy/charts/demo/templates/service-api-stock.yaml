apiVersion: v1
kind: Service
metadata:
  name: api-stock
  labels:
    app: api-stock
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: api-stock
