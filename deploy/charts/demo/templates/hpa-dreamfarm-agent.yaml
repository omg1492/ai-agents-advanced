apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dreamfarm-agent
  labels:
    app: dreamfarm-agent
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dreamfarm-agent
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
