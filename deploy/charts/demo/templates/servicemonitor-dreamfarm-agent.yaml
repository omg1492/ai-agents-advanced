apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dreamfarm-agent
  labels:
    app: dreamfarm-agent
    release: prometheus  # Required for kube-prometheus-stack to discover
spec:
  selector:
    matchLabels:
      app: dreamfarm-agent
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
