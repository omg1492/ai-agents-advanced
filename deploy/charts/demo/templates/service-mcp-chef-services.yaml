apiVersion: v1
kind: Service
metadata:
  name: mcp-chef-services
  labels:
    app: mcp-chef-services
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: mcp-chef-services
