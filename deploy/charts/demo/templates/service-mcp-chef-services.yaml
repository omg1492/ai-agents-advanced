apiVersion: v1
kind: Service
metadata:
  name: mcp-chef-services
  labels:
    app: mcp-chef-services
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: {{ .Values.staticIPResourceGroup | quote }}
    service.beta.kubernetes.io/azure-pip-name: {{ .Values.staticIPNames.mcpChefServices | quote }}
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: mcp-chef-services
