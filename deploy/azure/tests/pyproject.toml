[project]
name = "tests"
version = "0.1.0"
description = "Smoke tests for deployed infrastructure"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
    "colorama>=0.4.6",
    "fastmcp>=2.10.0",
]
