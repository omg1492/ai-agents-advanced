# Static Public IP addresses removed - now using Ingress Controller
# All services are exposed through a single NGINX Ingress Controller
# with hostname-based routing.
#
# The ingress IP is defined in ingress.tf
